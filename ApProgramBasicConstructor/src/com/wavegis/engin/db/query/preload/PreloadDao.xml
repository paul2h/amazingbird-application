<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wavegis.engin.db.query.preload.PreloadDaoConnector">

	<select id="getRawPreloadData" resultType="com.wavegis.model.PreloadData">
			SELECT 
				TRIM(BOTH ' ' FROM a.stno) AS stno 
				, 'level_raw_station' AS tableName 
			FROM 
				public.level_raw_station a 
			UNION 
			SELECT 
				TRIM(BOTH ' ' FROM a.stno) AS stno 
				, 'level_raw_station1' AS tableName 
			FROM 
				public.level_raw_station1 a 
			;
	</select>
</mapper>